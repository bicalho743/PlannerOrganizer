[server]
headless = true
address = "0.0.0.0"
port = 5000

# Theme configuration - opções para personalizar são apenas exemplos
[theme]
# Valores disponíveis para personalização (comentados para não aplicar ainda)
# primaryColor = "#2C3E50"  # Azul escuro profissional
# backgroundColor = "#FFFFFF"  # Branco como fundo para um visual limpo
# secondaryBackgroundColor = "#F8F9FA"  # Cinza muito claro para contraste
# textColor = "#212529"  # Tom escuro quase preto para texto
# font = "sans serif"  # Fonte padrão

# Arquivos de configuração adicionais
[browser]
gatherUsageStats = false
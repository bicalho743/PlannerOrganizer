Fazer as seguintes correções em uma única implementação:
1. Normalização do tipo de ação na função get_template
Converter para minúsculo antes de buscar: etapa = tipo_acao.lower().strip()
2. Mapeamento legado — tipos antigos → novos
pythonMAPEAMENTO_LEGADO = {
    "agradecimento": "agradecimento",
    "manutencao":    "acompanhamento",
    "manutençao":    "acompanhamento",
    "manutenção":    "acompanhamento",
    "follow-up":     "acompanhamento",
    "follow_up":     "acompanhamento",
    "feedback":      "feedback",
    "oportunidade":  "continuidade",
}
3. Atualizar os registros existentes no banco para os nomes novos
sqlUPDATE acoes_pos_org SET tipo_acao = 'acompanhamento' WHERE LOWER(tipo_acao) IN ('manutencao','manutenção','follow-up','follow_up');
UPDATE acoes_pos_org SET tipo_acao = 'continuidade'   WHERE LOWER(tipo_acao) = 'oportunidade';
UPDATE acoes_pos_org SET tipo_acao = 'ajuste_fino'    WHERE LOWER(tipo_acao) IN ('ajuste fino','ajuste_fino');
UPDATE acoes_pos_org SET tipo_acao = LOWER(tipo_acao) WHERE tipo_acao = UPPER(tipo_acao);
4. Corrigir os prazos dos registros existentes da Keila (proposta #104, encerramento 16/03/2026)
sqlUPDATE acoes_pos_org SET 
  data_prevista = '2026-03-23', tipo_acao = 'acompanhamento'
  WHERE proposta_id = 104 AND LOWER(tipo_acao) IN ('manutencao','manutenção','follow_up','acompanhamento');

UPDATE acoes_pos_org SET 
  data_prevista = '2026-04-15', tipo_acao = 'ajuste_fino'
  WHERE proposta_id = 104 AND LOWER(tipo_acao) IN ('follow-up','ajuste_fino');

UPDATE acoes_pos_org SET 
  data_prevista = '2026-04-30', tipo_acao = 'feedback'
  WHERE proposta_id = 104 AND LOWER(tipo_acao) = 'feedback';

UPDATE acoes_pos_org SET 
  data_prevista = '2026-05-15', tipo_acao = 'continuidade'
  WHERE proposta_id = 104 AND LOWER(tipo_acao) IN ('oportunidade','continuidade');
5. Garantir que novos projetos finalizados usem os D+ corretos
pythonETAPAS_POS_ORG = [
    {"etapa": "agradecimento", "nome": "Agradecimento", "dias_apos": 1},
    {"etapa": "acompanhamento","nome": "Acompanhamento","dias_apos": 7},
    {"etapa": "ajuste_fino",   "nome": "Ajuste fino",   "dias_apos": 30},
    {"etapa": "feedback",      "nome": "Feedback",      "dias_apos": 45},
    {"etapa": "continuidade",  "nome": "Continuidade",  "dias_apos": 60},
]
6. Corrigir o bug do ( — tratar template vazio antes de renderizar
pythontexto = template.get("texto", "") or ""
if not texto:
    texto = ""  # nunca renderizar None ou valor incompleto
texto_final = texto.replace("{nome}", nome_cliente or "cliente")